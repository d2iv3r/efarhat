apiVersion: v1
kind: Service
metadata:
  name: wil42-service
  namespace: dev
  labels:
    app.kubernetes.io/instance: efarhat-app
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: wil-playground
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8888