apiVersion: v1
kind: Service
metadata:
  name: wil-playground
  namespace: dev
  labels:
    app.kubernetes.io/instance: wil-playground
    app: wil-playground
spec:
  type: LoadBalancer
  selector:
    app: wil-playground
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8888